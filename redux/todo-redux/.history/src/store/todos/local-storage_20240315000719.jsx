export const loadState = () => {
    try {
        const savedState = localStorage.getItem('state')
        if(savedState === null){
            
        }
    } catch (error) {
        
    }
}

export const saveState = (state) => {
    
}